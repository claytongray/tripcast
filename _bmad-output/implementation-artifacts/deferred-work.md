# Deferred Work

## Deferred from: code review of story-1.1 (2026-06-29)

- Attacker can invalidate a victim's pending magic link by re-requesting it — every new request hard-deletes prior unconsumed tokens for that email (`app/Actions/RequestMagicLink.php:33`). Inherent to AC4's invalidate-prior-tokens rule and bounded by the per-email throttle; revisit only if abuse is observed.
- Frontend chrome remnant — the shared Inertia `sidebarOpen` prop and related starter-kit cruft (`app/Http/Middleware/HandleInertiaRequests.php`). Deferred to the Group B (frontend) review pass, which covers the Vue/UI surface of story 1.1.

## Deferred from: Epic 1 batch code review (2026-06-29)

- **Trip-level dedup** (story 1.4, `app/Actions/CreateTrip.php`) — no idempotency key/unique constraint on trips, so a true concurrent double-POST could insert duplicate trips. Mitigated now by the per-IP route throttle, immediate session-clear on commit, and the client `form.processing` disable. A DB unique on `(user_id, canonical_place_name, departure_date, return_date)` has soft-delete (re-add after delete) implications — revisit if duplicates are observed.
- **`firstOrCreate` user race** (story 1.4, `app/Actions/CreateTrip.php`) — SELECT-then-INSERT can hit the CI-unique email index under concurrent same-email submits (uncaught QueryException). Low likelihood; fix by catching the unique violation and re-fetching. (Tracked as an open action item on story 1.4.)
- **`WelcomeMail` + `SerializesModels`** (story 1.5, `app/Mail/WelcomeMail.php`) — if a trip is soft-deleted between queue and processing, model re-fetch throws `ModelNotFoundException`. No soft-delete path exists until Story 3.1; revisit then (pass scalars or tolerate the missing model).

## Deferred from: browser testing (2026-06-29)

- **Live location autocomplete (Google Places Autocomplete)** — destination is free-text resolved on submit; v1 deliberately chose "no 'did you mean?' picker" (DESIGN.md/EXPERIENCE.md), so there's no as-you-type suggestion dropdown. Adding it reverses that decision and needs the Places Autocomplete API (separate cost/setup). Revisit as a fast-follow if desired.
- **Persistent login indicator across all public pages** — Story-1.5-review added a login affordance to the landing top bar only; a shared public header (so the indicator shows on the confirm/auth pages too) is a small refactor, natural to fold into the Epic 3 dashboard top-bar work.

## Deferred from: scope/polish backlog (2026-06-30)

- **High-quality date picker for trip dates** — the trip-setup form (Story 1.2, `resources/js/pages/Landing.vue` + `TripSetupRequest`) uses plain date inputs. User wants a polished range picker (start/end) for the polish phase, not now. Requirements to brainstorm when scheduled: range selection with end ≥ start and no past start, strong mobile/touch UX, full accessibility (keyboard nav, screen-reader labels), and library-vs-custom. Promote to a proper story (`bmad-create-story`) when polish work begins; keeping date inputs simple until then means the swap stays a contained frontend change.

- **Dead-simple user feedback capture (lean-launch listening)** — goal: make it as easy as possible for early users to tell us anything, to drive lean post-launch iteration. **Already shipped:** per-digest one-tap thumbs (Story 2.6 — `helped`/`not_helpful`, signed email links → `app/Http/Controllers/EmailAction.php`, one row per `(trip_id, send_date)` in the `feedback` table). **To add (all three):** (1) **Site feedback form/widget** — always-available free-text feedback about the site (floating widget and/or a `/feedback` page); needs its own lightweight model/table (free-text body, optional contact email, source/page, nullable user/trip) — do NOT overload the digest `feedback` table, which is the structured digest-reaction metric. (2) **Post-trip "how was it?" email** — automated, sent after a trip's `return_date` asking how the trip + digests went; new scheduled job/command (cadence cousin of `SendDailyDigests`), login-free signed link to a short form, one-shot per trip (idempotency guard). (3) **Expand digest thumbs to capture optional text** — on the existing feedback confirmation page (`resources/js/pages/email/FeedbackConfirm.vue` / `FeedbackResult.vue`), let users add a free-text comment after tapping; store as a nullable `comment` column on `feedback` or in the new general-feedback table. Keep all three frictionless (no login, minimal fields). Deferred to backlog per user; promote to proper stories (`bmad-create-story`) when scheduled — natural Epic 2/3 lineage (reuses the signed-link + scheduled-command patterns).

- **WeatherAPI Pro+ upgrade — extended horizon via Forecast (14d) + Future (≤300d) API** — currently on a lower tier; the live provider (`app/Services/Weather/WeatherApiProvider.php`) only calls the `forecast.json` endpoint and the horizon is capped by `tripcast.forecast.horizon_days` (default 7, env `TRIPCAST_FORECAST_HORIZON_DAYS`). WeatherAPI **Pro+ ($25/mo or $270/yr)** unlocks **14-day Forecast** + **300-day-ahead Future Weather** + 5M calls + 365-day historical. Vision: for trip days within the 14-day window use `forecast.json` as today; for days beyond 14d (up to 300d) call the **Future API** and label those days **"Projected"** in the digest (distinct visual treatment from real forecast). Could also send an early "trip preview" email when a trip is ≤300 days out. Open questions before committing: (1) **accuracy of Future API** projected days — needs validation, these are climatology-style estimates not true forecasts; (2) how WeatherAPI derives future weather; (3) UX wording/disclaimer for "Projected" so it doesn't read as a real forecast. Deliberately deferred on **cost** ($25/mo) until closer to a UX need — user wants to wait before making the jump but considers it likely necessary for customer UX. Implementation seam is clean: extend the `WeatherProvider` port to select Forecast-vs-Future by day offset, mapping both into the existing `ForecastDay`/`Forecast` shapes with a `projected` flag. Docs: https://www.weatherapi.com/docs/ — Future API: https://www.weatherapi.com/docs/#apis-future. Promote to a proper story (`bmad-create-story`, Epic 2 weather-provider lineage) when scheduled.

## Deferred from: code review of Epic 3 (2026-06-30)

- **Admin view unbounded query** (story 3.4) — `AdminController@index` loads all trips across users with eager-loaded `emailLogs` via `->get()`, no pagination/limit. N+1 avoided but result set grows without bound. Add a pagination/limit seam before the monitoring view sees real data volume. [app/Http/Controllers/AdminController.php:110-135]
- **Just-confirmed zero-trip reassurance message** (story 3.2) — `MagicLinkController@consume` dropped the "You're all set" flash for the confirmed-with-no-trips case. Near-unreachable because signup always creates a pending_trip; revisit only if a trip-less confirmation path is ever introduced. [app/Http/Controllers/Auth/MagicLinkController.php:164-184]
- **Resume bypasses the active-trip cap** (story 3.3) — `resume()` flips a paused trip to active via `transitionTo()` with no occupancy check, so a capped user can resume past the cap (AD-15 cost control). Deferred: may remove the pause feature entirely, which would make this moot. Revisit only if pause stays AND paid plans launch. [app/Http/Controllers/TripController.php:388-395]

## Deferred from: code review of Epic 4 (2026-06-30)

- **Purge unbounded UPDATE (no chunking)** (story 4.1) — `PurgeForecastHistory` runs one `whereIn(subquery)->update()`; under a large backlog it can hold locks on `email_logs` contending with in-flight send claims. Add `chunkById` batching before real data volume. [app/Actions/PurgeForecastHistory.php:42-47]
- **Unit-aware notable-temp threshold** (story 4.2) — `narration.notable.temp` (default 10) gates both °C and °F identically, so Celsius users get a ~18°F effective bar and fewer temperature lines. Decide a unit-aware threshold (e.g. separate C/F values, or normalize). [app/Services/Narration/NarrationDiffer.php:578,601-604]
- **Duplicate-date dedup in NarrationDiffer::byDate** (story 4.2) — last-wins on duplicate `date` keys in a snapshot's `days`; add detection/dedup if the provider ever emits dupes. Low likelihood. [app/Services/Narration/NarrationDiffer.php:626-634]
- **narrator:compare null-safe user** (story 4.2) — `$trip->user->temperature_unit` can null-deref on an orphaned trip user (`withTrashed()` covers trips, not the user relation). Console tool only. [app/Console/Commands/CompareNarrators.php:102]

## Deferred from: code review of story-1.1 (2026-07-01)

- **Cross-flow single-live-token collision** (story 1.1 / resend reuse) — login, signup, and the public sample all share one live-token slot per user (`RequestMagicLink::issue()` deletes ALL unconsumed tokens for the user). An unrelated `issue()` (e.g. a sample request) deletes another flow's already-emailed valid link, and the session-stashed resend pointer can't protect it. Pre-existing, inherent to AD-6's delete-prior-unconsumed rule; extends the existing "attacker can invalidate a victim's pending link" defer. Would need a per-purpose token scope (or not deleting cross-purpose tokens) to resolve. [app/Actions/RequestMagicLink.php:49]

## Deferred from: code review of Epic 8 (2026-07-01)

- **CTR reads 0.0% with nonzero clicks near a window edge** — when a slug's impression falls just outside the selected 7/30/90-day window but a click falls inside it, the catalog/promos row shows `impressions=0, clicks≥1, ctr=0.0%`. Inherited windowed-analytics semantics from the Story 7.6 `PromoAnalytics` fold (not introduced by 8.5). Low impact; revisit if it confuses reporting. [app/Services/Metrics/PromoAnalytics.php]
- **Admin restore path for retired (soft-deleted) items** — the catalog index excludes trashed rows and `edit()` 404s on them, so a retired item can't be restored from the UI. Add a "Retired" view/filter + a `restore` route/action + button. Deferred from Epic 8 review (option 1: copy softened to not promise restore). [routes/web.php; app/Http/Controllers/PromoItemController.php]

## Deferred from: Story 9.7 (2026-07-02)

- **Maskable app-icon variants (the one missing brand asset)** — `site.webmanifest` declares the 192/512 icons with `"purpose": "any"` only: the sun+wave mark touches the icon edges, so Android's maskable crop would clip the rays. When the designer exports padded variants (mark at ~60% scale on a solid `#F6F9FC` or `#2563A6` field, 192 + 512), add them to the manifest as separate `"purpose": "maskable"` entries alongside the existing `"any"` ones. Guard note: `tests/Feature/Landing/BrandAssetsTest.php` asserts no icon claims `maskable` — relax that assertion to "maskable entries must be the padded files" when they land. Related: `public/brand-assets/twitter-card.png` is byte-identical to `brand-assets/og-image.png` (1200×630 brand lockup); both are deliberately unused — the served `/og-image.png` stays the 2400×1260 product-shot preview (user decision, 2026-07-02).
