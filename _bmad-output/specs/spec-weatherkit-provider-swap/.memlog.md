---
topic: Swap weather provider from WeatherAPI.com to Apple WeatherKit (REST), core swap + attribution; alerts and condition/precip reconciliation deferred
updated: 2026-07-04T10:49
---

- (event) CREATE from rich in-chat intent (8 confirmed decisions); express distill, no elicitation — input rich per Spec Law.
- (direction) Diagnosis established: our extraction is faithful; rendered high == round(day.maxtemp_f). Bias is upstream in WeatherAPI's maxtemp_f on hot/humid inland days (heat-index bleed). Fix is at the provider, not our code.
- (decision) Adopt Apple WeatherKit REST as the weather provider — it IS the Apple Weather data we benchmark against, global, alerts-capable, 500k calls/mo free with the $99/yr membership.
- (decision) Scope = core provider swap + mandatory Apple Weather attribution in the digest. Alerts (Bug 2b) and condition/precip reconciliation 'Sunny·58%' (Bug 2a) are separate, deferred specs.
- (capability) CAP-1 WeatherKitProvider implements the existing WeatherProvider port, selected by config/binding; no change to Forecast/ForecastDay/snapshot/ForecastRows/templates.
- (capability) CAP-2 Rendered daily high equals WeatherKit air-temp max for the destination-local day (fixes the 5-8F inflation); no heat-index/feels-like value ever surfaces as the high.
- (capability) CAP-3 Adapter converts WeatherKit metric/SI to the DTO's dual-unit temps and int percents (C->F; precip/humidity 0-1 -> 0-100); preserves FR-7 limited-not-fabricated.
- (capability) CAP-4 Feels-like sourced from peak hourly temperatureApparent (same pattern as today's peakFeelsLike); null when absent, never makes a day limited.
- (capability) CAP-5 conditionCode PascalCase enum -> human label that feeds existing WeatherEmoji unchanged.
- (capability) CAP-6 ES256 JWT auth via firebase/php-jwt, cached ~1h, with the non-standard id header (Team.Service) and iss=Team / sub=Service claims; credential/signing failures surface as WeatherProviderFailedException.
- (capability) CAP-7 Destination IANA timezone resolved from coords via Google Time Zone API (existing GOOGLE_GEOCODING_KEY), cached, passed as WeatherKit timezone input; reusable to fill trips.destination_timezone for the timezone-aware-send-time feature.
- (capability) CAP-8 Apple Weather attribution (logo + legal link, inlined) rendered in the digest footer whenever the active provider is WeatherKit; mark unaltered.
- (constraint) Downstream contract frozen: adapter-only swap; DTO shape, snapshot serialization, and rendering must not change.
- (constraint) WeatherKit returns metric/SI only with no unit param — adapter must convert C->F and scale 0-1 decimals.
- (constraint) timezone is effectively required; omitting it defaults daily rollups to GMT and misaligns the very daily high we are fixing.
- (constraint) JWT must be ES256 AND include the non-standard id header (Team.Service) or requests 401 — the #1 failure mode.
- (constraint) Attribution is legally mandatory wherever WeatherKit data shows; logo must be inlined (email clients strip remote images/CSS).
- (constraint) Credentials via env only (TEAM_ID/SERVICE_ID/KEY_ID/PRIVATE_KEY .p8); .p8 never logged or committed.
- (constraint) Only new dependency permitted is firebase/php-jwt (approved); no other deps.
- (constraint) Hard cutover (pre-launch): build behind config, test locally with MAIL_MAILER=log, then push; no shadow-compare.
- (constraint) Stay within 500k calls/mo free tier: cache the JWT and the resolved timezone.
- (decision) Non-goals: alerts; condition/precip reconciliation; building the timezone-aware-send-time scheduler/column (this only ships the shared resolver); WeatherAPI as long-term fallback/multi-provider blend; forecastNextHour/AQI/other datasets; editable units or per-user provider choice.
- (assumption) User's 'Great' approved both firebase/php-jwt and the Google Time Zone API for destination tz.
- (assumption) WeatherKit daily humidity taken from the daytimeForecast day-part (not day root) — verify against a live payload once credentials exist.
- (question) Is the Google Time Zone API enabled/billed on GOOGLE_GEOCODING_KEY? Must be enabled in Cloud console or resolution 403s.
- (question) Fallback when timezone resolution fails: user.timezone, UTC, or abort? Decision bends CAP-7 success.
- (question) Store the .p8 as raw env contents or a file path referenced by env?
- (event) Self-validate Pass 1 (Coherence, Spec Law 1-6,8): PASS. 8 capabilities each carry intent+success; constraints all rule out real choices; 6 non-goals; success signal is locally demonstrable via MAIL_MAILER=log. CAP-1/CAP-6 name the port and ES256/id-header — allowed as load-bearing external contract, deep HOW kept in companion.
- (event) Self-validate Pass 2 (Preservation): PASS. All 8 confirmed decisions landed (CAP-1..8 + constraints); Bug 2a/2b routed to Non-goals; firebase/php-jwt, Google Time Zone API, metric-only conversion, conditionCode map, feels-like peak, attribution, credentials, 500k tier, and the tz-source collision fix all preserved across SPEC.md + weatherkit-integration.md. No load-bearing drops.
- (event) Spec finalized: SPEC-weatherkit-provider-swap, 8 capabilities, 1 spec-authored companion (weatherkit-integration.md), 2 assumptions, 3 open questions.
- (decision) CONFIRMED: firebase/php-jwt approved for ES256 signing (was assumption).
- (event) Q1 RESOLVED: Google Time Zone API is enabled on GOOGLE_GEOCODING_KEY — live test returned status OK, America/New_York for Kennett Square coords.
- (decision) Q3 RESOLVED: APPLE_WEATHERKIT_PRIVATE_KEY holds a file PATH to the .p8 (adapter reads the file, resolved via base_path); *.p8 added to .gitignore so the key cannot be committed.
- (decision) Q2 RESOLVED: on timezone-resolution failure, fall back to config default America/New_York and log a warning — never GMT, never abort the send. Rare path; error is a few-hour window shift, non-systematic. Overridable.
- (event) Setup verified green: 4 APPLE_WEATHERKIT_* env vars set (Team 10c, Key 10c, Service 24c, PrivateKey=path); .p8 is valid PKCS#8 PEM (257 bytes), git-ignored.
- (direction) User: don't depend on Google every send — store the zone. Correct: resolve once per trip, persist, reuse.
- (capability) CAP-9 Persist destination IANA timezone on trips.destination_timezone (new nullable column): resolved+written on first forecast, reused on every later send with no Google call; cache is secondary. Google Time Zone API is hit at most once per trip.
- (decision) Scope change: this spec now OWNS the trips.destination_timezone column + populate (moved out of Non-goals); the timezone-aware-send-time feature consumes it. WeatherProvider::fetchForecast gains an optional $timezone param so the caller passes the persisted zone; sample/no-trip path lets the provider resolve+cache.
- (event) SMOKE TEST PASSED (live): all 3 locations HTTP 200; auth (ES256+id header) works. Highs Kennett 97F / Lewistown 93F / Dewey 86F vs old WeatherAPI 105/101/87 — CAP-2 proven, inland inflation gone, coastal control accurate.
- (event) Assumption RESOLVED via live payload: daily humidity is on days[].daytimeForecast.humidity (0-1); absent at day root. conditionCode came back 'Thunderstorms' w/ precip 0.52-0.58 — internally consistent (WeatherKit incidentally avoids WeatherAPI's 'Sunny 58%' contradiction). temperatureApparent peak > air temp (correct humid relationship).
- (decision) CAP-9 timing REFINED (user): resolve+persist trips.destination_timezone at TRIP CREATION (right after geocoding, outside the DB txn per AD-8), NOT lazily on first forecast — the welcome email embeds the first tripcast, so the zone must exist before any send. Reuses the Google call already made at creation-time geocoding.
- (event) Implementation plan written: docs/superpowers/plans/2026-07-04-weatherkit-provider-swap.md — 10 bite-sized TDD tasks covering CAP-1..9; coordinates with the timezone-aware-send-time plan (supersedes its T3/T4, banner added).
- (event) BMAD paper trail created: Epic 11 (Weather provider — Apple WeatherKit migration) registered via 3 story files (11-1 provider behind the port, 11-2 destination-timezone capture at creation, 11-3 attribution + cutover) all ready-for-dev in sprint-status.yaml, plus a non-destructive Epic-List append to epics.md (Epic 10 precedent). Superpowers plan retained as the implementation reference.
